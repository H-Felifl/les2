{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <h1>Hello {{ name }}</h1>

        <div class="row">
            <div class="col-4">
                <img class="img-fluid" src="{{ asset("img/duck2.png") }}" alt="">

            </div>
            <div class="col-8"></div>
        </div>

        <a href="{{ path('app_donald') }}">Donald Duck</a>

    </div>





{% endblock %}